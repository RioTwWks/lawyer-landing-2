{
  "name": "lawyer-landing-2",
  "version": "2.0.0",
  "private": true,
  "description": "Лендинг ООО «Дело Правое» — вариант 2 (строгий красно-жёлтый стиль)",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./css/input.css -o ./css/styles.css --watch",
    "build": "npx @tailwindcss/cli -i ./css/input.css -o ./css/styles.css --minify"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
